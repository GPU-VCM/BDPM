### Configure

* `cd ${PATH}/VCMPathTracer`
* `mkdir build`
* `cd build`
* `cmake-gui ..`
* `configure`
* `generate`
* Double click *VCM-PathTracer.sln*
* Set *optixPathTracer* as start-up project
* Run